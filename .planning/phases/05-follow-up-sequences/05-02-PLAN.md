---
phase: 05-follow-up-sequences
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - app/api/sequence-config/route.ts
  - app/dashboard/sequence/page.tsx
  - app/dashboard/layout.tsx
autonomous: false
requirements:
  - MAIL-03
  - SCRP-06

must_haves:
  truths:
    - "User can view and update follow-up sequence config (max follow-ups and interval days) from the dashboard"
    - "Sequence config changes are validated (0-10 follow-ups, 1-30 day interval) and persisted to DB"
    - "User can navigate to the scrape trigger page from the dashboard sidebar"
    - "Scrape trigger page is functional (form submits, job status polls)"
    - "supabase db push creates the sequence_config table in the live database"
  artifacts:
    - path: "app/api/sequence-config/route.ts"
      provides: "GET and PATCH endpoints for sequence config"
      exports: ["GET", "PATCH"]
    - path: "app/dashboard/sequence/page.tsx"
      provides: "Sequence configuration UI page"
      contains: "sequence"
    - path: "app/dashboard/layout.tsx"
      provides: "Dashboard nav with Sekwencje link"
      contains: "Sekwencje"
  key_links:
    - from: "app/dashboard/sequence/page.tsx"
      to: "/api/sequence-config"
      via: "fetch GET on mount, PATCH on save"
      pattern: "fetch.*api/sequence-config"
    - from: "app/api/sequence-config/route.ts"
      to: "lib/queries/sequence-config.ts"
      via: "getSequenceConfig and updateSequenceConfig calls"
      pattern: "getSequenceConfig|updateSequenceConfig"
---

<objective>
Deliver the sequence configuration dashboard UI, the API route, run supabase db push, and verify the scrape trigger page (SCRP-06). After this plan, users can configure follow-up parameters from the web dashboard and the full automated pipeline is live.

Purpose: MAIL-03 requires user-configurable follow-up count and intervals. SCRP-06 requires scrape jobs to be triggerable from the dashboard (already built in Phase 3, needs verification and nav presence).

Output: Sequence config API route, dashboard page, db push, verified scrape trigger page, full pipeline checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-follow-up-sequences/05-RESEARCH.md
@.planning/phases/05-follow-up-sequences/05-01-SUMMARY.md

<interfaces>
<!-- Contracts created by Plan 01 that this plan consumes -->

From lib/queries/sequence-config.ts (created in Plan 01):
```typescript
export async function getSequenceConfig(): Promise<SequenceConfig>;
export async function updateSequenceConfig(maxFollowUps: number, intervalDays: number): Promise<void>;
```

From lib/db/types.ts (extended in Plan 01):
```typescript
export interface SequenceConfig {
  id: number
  max_follow_ups: number
  interval_days: number
  updated_at: string
}
```

From app/dashboard/layout.tsx (existing):
```typescript
// Server Component with sidebar nav containing: Leady, Szablony, Scraping
// Must add: Sekwencje link to /dashboard/sequence
```

From app/dashboard/scrape/page.tsx (existing from Phase 3):
```typescript
// TriggerScrapeForm component with /api/scrape POST + /api/scrape/[jobId] polling
// Already linked in sidebar nav as "Scraping"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sequence config API route + dashboard page + nav link + db push</name>
  <files>
    app/api/sequence-config/route.ts,
    app/dashboard/sequence/page.tsx,
    app/dashboard/layout.tsx
  </files>
  <read_first>
    lib/queries/sequence-config.ts,
    lib/db/types.ts,
    app/dashboard/layout.tsx,
    app/dashboard/templates/page.tsx,
    app/api/sequence-config/route.ts,
    .planning/phases/05-follow-up-sequences/05-RESEARCH.md
  </read_first>
  <action>
    1. Create `app/api/sequence-config/route.ts`:
       - Import NextResponse from 'next/server'
       - Import z from 'zod' (zod 4.x — use z.object syntax)
       - Import getSequenceConfig, updateSequenceConfig from '@/lib/queries/sequence-config'
       - Define UpdateSchema = z.object({ max_follow_ups: z.number().int().min(0).max(10), interval_days: z.number().int().min(1).max(30) })
       - GET handler: call getSequenceConfig(), return NextResponse.json(config)
       - PATCH handler: parse body with UpdateSchema.safeParse, return 400 on failure, call updateSequenceConfig, return { success: true }
       - T-05-01: Zod validation caps max_follow_ups at 10 and interval_days minimum 1 to prevent spam/DoS
       - T-05-02: interval_days >= 1 prevents immediate send loops

    2. Create `app/dashboard/sequence/page.tsx`:
       - 'use client' directive (needs useState, useEffect, form interaction)
       - On mount: fetch GET /api/sequence-config, populate form state
       - Form with two fields:
         a. "Liczba follow-upow" — number input, min=0, max=10, bound to maxFollowUps state
         b. "Odstep miedzy wiadomosciami (dni)" — number input, min=1, max=30, bound to intervalDays state
       - Save button: PATCH /api/sequence-config with { max_follow_ups, interval_days }
       - Show success/error feedback after save (simple inline message, no toast library needed)
       - Show current config values and last updated timestamp
       - Display explanatory text: "Kazdy lead po wyslaniu cold emaila otrzyma automatyczne follow-upy zgodnie z ta konfiguracja. Odpowiedz lub opt-out natychmiast zatrzymuje sekwencje."
       - Styling: match existing dashboard pages (Tailwind classes, consistent with templates/page.tsx)
       - Add a warning section: "Upewnij sie, ze aktywne szablony emaili istnieja dla kazdej pozycji sekwencji (1, 2, ...) w zakladce Szablony."

    3. Modify `app/dashboard/layout.tsx`:
       - Add NavLink for sequence config: `<NavLink href="/dashboard/sequence">Sekwencje</NavLink>`
       - Place it after "Szablony" and before "Scraping" in the nav order

    4. Run `npx supabase db push` to deploy the sequence_config migration to the live Supabase project (ref: uwuicdilargmuvhfdwue).
       - If db push fails due to auth, this becomes a checkpoint — log the error clearly.

    5. Verify scrape page accessibility (SCRP-06):
       - Confirm `app/dashboard/scrape/page.tsx` exists and compiles
       - Confirm "Scraping" NavLink exists in layout.tsx (it already does at line 20)
       - Run `npx tsc --noEmit` to verify all pages compile cleanly
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - /api/sequence-config GET returns current config JSON
    - /api/sequence-config PATCH validates input (rejects interval_days=0, max_follow_ups=99)
    - Dashboard sequence page renders with form fields and save button
    - Dashboard layout has "Sekwencje" nav link pointing to /dashboard/sequence
    - Scrape page link ("Scraping") present in nav and page exists
    - supabase db push succeeds (or clear error if auth needed)
    - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>
    Sequence config UI is accessible from the dashboard. API route validates and persists config. DB migration deployed. Scrape trigger page is navigable.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete follow-up pipeline and dashboard UI</name>
  <files>app/dashboard/sequence/page.tsx, app/dashboard/layout.tsx, app/dashboard/scrape/page.tsx</files>
  <action>
    Human verifies the complete follow-up sequence pipeline built across Plans 01 and 02:
    1. Sequence configuration page at /dashboard/sequence with configurable follow-up count (0-10) and interval (1-30 days)
    2. Follow-up scheduling: after a cold email sends, the system automatically schedules follow-ups using pg-boss deferred jobs
    3. Follow-up worker: sends follow-ups only to leads with active status (contacted/followed_up), stops on reply/opt-out
    4. sendColdEmail now supports follow-up sends (targetStatus=FOLLOWED_UP, correct sequence_number)
    5. Scrape trigger page accessible from dashboard sidebar (SCRP-06)
    6. sequence_config table deployed to Supabase

    Steps to verify:
    1. Start the dev server: `npm run dev`
    2. Navigate to http://localhost:3000/dashboard
    3. Verify sidebar has four links: Leady, Szablony, Sekwencje, Scraping
    4. Click "Sekwencje" — verify config form loads with defaults (2 follow-ups, 5 days)
    5. Change values (e.g., 3 follow-ups, 7 days) and click Save — verify success message appears
    6. Refresh page — verify saved values persist
    7. Try invalid values (interval=0) — verify form rejects
    8. Click "Scraping" — verify scrape trigger form loads (SCRP-06)
    9. Check terminal for: "[follow-up-worker] registered follow-up-send worker" at startup
  </action>
  <verify>Manual visual and functional verification by user</verify>
  <done>User confirms dashboard UI works, config persists, scrape page is accessible, follow-up worker is registered at startup. Type "approved" or describe issues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> /api/sequence-config | User-supplied config values cross trust boundary |
| Browser -> /api/scrape | User-supplied scrape parameters (existing from Phase 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | /api/sequence-config PATCH | mitigate | Zod schema: max_follow_ups z.number().int().min(0).max(10), interval_days z.number().int().min(1).max(30) |
| T-05-02 | Denial of Service | interval_days=0 causing instant sends | mitigate | Zod rejects min(1); follow-up.ts also guards sequenceStep >= 1 |
| T-05-07 | Spoofing | Unauthenticated access to config API | accept | Single-user local tool per project constraints; dashboard has no auth (noted as TODO in layout.tsx); acceptable for v1 |
</threat_model>

<verification>
After both tasks complete:
1. `npx vitest run` — full suite green
2. `npx tsc --noEmit` — zero errors
3. Dashboard renders all four nav items
4. Sequence config page loads, saves, and persists
5. Scrape page accessible and functional
6. Follow-up worker registered at startup (visible in terminal logs)
</verification>

<success_criteria>
- User can configure follow-up count and interval from /dashboard/sequence (MAIL-03 config requirement)
- API validates input preventing spam/DoS (max 10 follow-ups, min 1 day interval)
- Scrape trigger page accessible from dashboard nav (SCRP-06)
- sequence_config table deployed to live Supabase
- All tests pass, TypeScript clean
</success_criteria>

<output>
After completion, create `.planning/phases/05-follow-up-sequences/05-02-SUMMARY.md`
</output>
